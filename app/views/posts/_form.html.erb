<%= form_with(model: @post,url: choose_new_or_edit, local: true) do |f| %>
  <% if @post.errors.any? %>
    <div id="error_explanation" class="alert alert-danger">
      <ul>
        <% @post.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <table class="table">
    <tr>
      <th class="col-md-4 col-xs-4"><%= f.label :title,"題名" %></th>
      <td class="col-md-8 col-xs-8"><%= f.text_field :title %></td>
    </tr>
      <th class="col-md-4 col-xs-4"><%= f.label :content,"内容" %></th>
      <td class="col-md-8 col-xs-8"><%= f.text_area :content %></td>
    <tr>
      <th class="col-md-4 col-xs-4"><%= f.label :post_image,"画像" %></th>
      <td class="col-md-8 col-xs-8">
        <%= image_tag(@post.post_image.url) if @post.post_image && @post.post_image.url %>
        <%= f.file_field :post_image %>
        <%= f.hidden_field :post_image_cache %>
      </td>
    <tr>
      <th></th>
      <td><%= f.submit "投稿" %></td>
    </tr>
  </table>
<% end %>